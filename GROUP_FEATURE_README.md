# 组队功能使用说明

## 功能概述

组队功能允许用户创建和管理团队，共享交易记录，进行团队财务管理。该功能分为两个阶段实现：

### 第一阶段：基础组管理功能 ✅
- 组列表页面 - 显示用户加入的所有组
- 创建组页面 - 创建新组
- 组详情页面 - 显示组基本信息和成员
- 加入组功能 - 通过组ID加入组

### 第二阶段：交易管理功能 ✅
- 组交易列表 - 显示组内所有交易
- 交易共享功能 - 从个人交易共享到组
- 手动添加交易 - 直接在组内添加交易
- 交易筛选和搜索 - 按时间、分类、成员筛选

## 页面结构

```
/groups                    # 组列表页面
├── /[id]                 # 组详情页面
│   ├── /transactions     # 组交易列表
│   ├── /share           # 交易共享页面
│   └── /add-transaction # 添加交易页面
└── /test                # 测试页面
```

## 主要功能

### 1. 组管理

#### 创建组
- 访问 `/groups` 页面
- 点击"创建组"按钮
- 填写组名称、描述、最大成员数
- 提交创建

#### 加入组
- 访问 `/groups` 页面
- 点击"加入组"按钮
- 输入组ID
- 确认加入

#### 查看组详情
- 在组列表中点击任意组卡片
- 查看组基本信息、成员列表、统计数据
- 管理组内功能

### 2. 交易管理

#### 查看组交易
- 在组详情页面点击"查看交易"
- 查看组内所有共享的交易记录
- 使用筛选器按分类、日期、成员筛选

#### 共享个人交易
- 在组交易页面点击"共享交易"
- 选择要共享的个人交易记录
- 批量共享到组内

#### 添加组交易
- 在组交易页面点击"添加交易"
- 填写交易信息（描述、金额、分类、日期等）
- 直接添加到组内

### 3. 高级功能

#### 生成AI报告
- 在组详情页面点击"生成AI报告"
- 系统将分析组内交易数据
- 生成智能财务分析报告

#### 导出数据
- 在组详情页面点击"导出数据"
- 导出组内所有交易数据
- 支持多种格式导出

#### 查看统计
- 在组详情页面点击"查看统计"
- 查看组内交易统计数据
- 包括总金额、交易数量、平均金额等

## API接口

### 组管理API
- `POST /api/group/create/` - 创建组
- `GET /api/group/list/` - 获取用户组列表
- `GET /api/group/{group_id}/` - 获取组详情
- `POST /api/group/join/` - 加入组
- `POST /api/group/{group_id}/leave/` - 退出组

### 交易管理API
- `POST /api/group/share_transactions/` - 共享交易
- `GET /api/group/{group_id}/transactions/` - 获取组交易
- `POST /api/group/{group_id}/ai_reports/generate/` - 生成AI报告
- `GET /api/group/{group_id}/statistics/` - 获取统计数据
- `GET /api/group/{group_id}/export/` - 导出数据

## 技术实现

### 状态管理
使用 React Context 进行状态管理：
- `GroupContext` - 管理组相关状态
- 包含组列表、当前组、加载状态等

### API服务
- `groupService.js` - 封装所有组相关API调用
- 统一的错误处理和认证管理

### 组件结构
- 页面组件：负责页面布局和用户交互
- 功能组件：可复用的UI组件
- 状态组件：与状态管理交互的组件

## 使用流程

### 新用户流程
1. 登录系统
2. 访问组队页面 (`/groups`)
3. 创建新组或加入现有组
4. 开始共享交易记录

### 日常使用流程
1. 查看组内交易记录
2. 共享新的个人交易
3. 查看组统计和AI报告
4. 管理组内事务

## 注意事项

1. **权限管理**：组管理员拥有更多权限，可以管理组成员
2. **数据安全**：组内交易数据对所有成员可见，请谨慎共享敏感信息
3. **API限制**：某些功能需要后端API支持，请确保后端服务正常运行
4. **浏览器兼容**：建议使用现代浏览器以获得最佳体验

## 故障排除

### 常见问题
1. **无法创建组**：检查网络连接和用户权限
2. **交易共享失败**：确认选择的交易记录有效
3. **页面加载缓慢**：检查网络连接和服务器状态

### 调试方法
1. 访问 `/groups/test` 页面查看状态信息
2. 检查浏览器控制台的错误信息
3. 确认API接口返回正确的数据格式

## 后续开发计划

### 第三阶段：高级功能
- 组内聊天功能
- 预算管理
- 费用分摊
- 组内通知系统

### 第四阶段：移动端支持
- 响应式设计优化
- 移动端专用功能
- PWA支持

## 联系方式

如有问题或建议，请联系开发团队。

